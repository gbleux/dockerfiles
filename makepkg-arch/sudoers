User_Alias              BUILDERS = @USER@

Defaults                env_keep += "DISPLAY HOME"
Defaults>root           !set_logname
Defaults:BUILDERS       !lecture

BUILDERS                ALL = (ALL) NOPASSWD: ALL
